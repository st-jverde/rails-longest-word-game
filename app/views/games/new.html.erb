<div class="header">
  <h1>New Game</h1>
</div>

<div class="container">
  <% @letters.each do |letter| %>
    <div class="cube"><%= letter %></div>
  <% end %>
</div>

<div class="form">
  <form action="/score" method="post" data-turbo="false">
    <input type="text" class="form-control" name="word" placeholder="Make a word!">
    <button type="submit" class="btn">Submit</button>
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  </form>
</div>
